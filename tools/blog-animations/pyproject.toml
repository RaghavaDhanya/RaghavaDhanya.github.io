[project]
name = "blog-animations"
version = "0.1.0"
description = "Render blog post animations (HTML + SVG) to GIFs via Playwright."
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.45",
    "imageio>=2.34",
    "pillow>=10.0",
    "numpy>=1.26",
]
